<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="AppTheme_Dark"> <!-- Áp dụng các thuộc tính tùy chỉnh cho giao diện tối -->
        <item name="android:windowBackground">@android:color/black</item>
        <!-- Thiết lập các thuộc tính khác cho chế độ dark -->
        <!-- ... -->
    </style>

    <style name="AppTheme_Light">
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Thiết lập các thuộc tính khác cho chế độ sáng -->
        <!-- ... -->
    </style>

</resources>